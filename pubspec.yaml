name: paseto_dart
description: Paseto is everything you love about JOSE (JWT, JWE, JWS) without any of the many design deficits that plague the JOSE standards.
version: 1.0.0
homepage: https://github.com/nogipx/paseto_dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  pointycastle: ^4.0.0
  equatable: ^2.0.7
  meta: ^1.16.0
  ed25519_edwards: ^0.3.1
  convert: ^3.1.2

dev_dependencies:
  hex: ^0.2.0
  lints: ^5.0.0
  test: ^1.24.0